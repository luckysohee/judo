# 네이버 블로그 크롤러 필요 패키지
playwright==1.58.0
beautifulsoup4==4.14.3
asyncio==4.0.0
lxml==6.0.2
requests==2.32.5

# 데이터 처리
# numpy 2.4.x 휠은 Python 3.10+ 위주 — macOS 기본 python3(3.9)는 2.0.x 대만 설치됨
numpy==2.4.2; python_version >= "3.10"
numpy==2.0.2; python_version < "3.10"
pandas==2.3.3

# 기타 유틸리티
python-dateutil==2.9.0.post0
pytz==2025.2

# 추천 API (recommendation/*.py, server GET /recommend)
python-dotenv>=1.0.0
supabase>=2.3.0
# supabase → realtime 이 websockets.asyncio 를 씀. 구버전(12.x 이하)만 있으면 ModuleNotFoundError
websockets>=13.0
openai>=1.40.0
httpx>=0.27.0

# recommendation 단위 테스트: pytest recommendation/test_recommend.py
pytest>=8.0.0
