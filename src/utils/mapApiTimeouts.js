/** 지도 뷰포트 — 해외 Railway + Supabase(Mumbai) 왕복 고려 */
export const MAP_PLACES_FETCH_TIMEOUT_BOOT_MS = 16_000;
export const MAP_PLACES_FETCH_TIMEOUT_MS = 12_000;
export const MAP_DENSITY_FETCH_TIMEOUT_MS = 10_000;

/** 이 시간 넘게 걸릴 때만 '불러오는 중' 칩 노출 (짧은 pan 깜빡임 방지) */
export const MAP_VIEWPORT_LOADING_UI_DELAY_MS = 450;
